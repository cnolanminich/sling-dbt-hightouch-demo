version: 2

sources:
  - name: raw
    description: Raw tables ingested via Sling
    schema: raw
    tables:
      - name: customers
        description: Customer data from source database
      - name: orders
        description: Order data from source database
      - name: products
        description: Product catalog data
